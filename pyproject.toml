[project]
name = "stateful-firewall-engine"
version = "0.1.0"
description = "A Python firewall engineering and packet inspection lab."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"
